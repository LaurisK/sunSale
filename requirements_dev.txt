pytest>=7.0
pytest-asyncio>=0.21
# ruff and mypy are pinned exactly, not floated. Both are run as CI gates, and
# both add rules in minor releases -- floating them means a new upstream release
# turns master red with no change on our side, which is how a green branch stops
# meaning anything. Bump these deliberately, with the fixes in the same commit.
ruff==0.15.21
mypy==2.2.0
pytest-homeassistant-custom-component>=2024.1.0
# TUI stack for tools/checks (the integration-check harness); also pulled in at
# test collection via test_baked_observed_check.py → tools.integration_check.
textual>=0.50
# Public-holiday calendars (the integration's one runtime requirement, see
# manifest.json); tests that need the real package skip without it.
holidays>=0.35
# Parses upstream solis_modbus's services.yaml in the contract guard
# (tests/test_solis_modbus_contract.py); that module skips without it.
pyyaml>=6.0
